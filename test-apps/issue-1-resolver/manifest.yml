modules:
  jira:globalPage:
    - key: forge-sandbox
      resource: web
      resolver:
        function: resolver
      layout: basic
      title: Forge Sandbox
  function:
    - key: resolver
      handler: index.handler
resources:
  - key: web
    path: web
permissions:
  content:
    styles:
      - unsafe-inline
app:
  id: ari:cloud:ecosystem::app/register_to_get_id
